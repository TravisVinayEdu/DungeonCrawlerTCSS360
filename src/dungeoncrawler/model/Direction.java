package dungeoncrawler.model;

/**
 * Cardinal movement directions used for dungeon doors and hero movement.
 */
public enum Direction {
    /** Movement toward the previous row. */
    NORTH,
    /** Movement toward the next row. */
    SOUTH,
    /** Movement toward the previous column. */
    WEST,
    /** Movement toward the next column. */
    EAST;
}
